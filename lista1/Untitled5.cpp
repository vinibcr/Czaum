#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main (){
	char*nome;
	char*prof;
	int id;
	float sal;
	scanf("%s",&nome);
	scanf("%s", &nome);
	scanf("%d", id);
	scanf("%f", sal);
}
